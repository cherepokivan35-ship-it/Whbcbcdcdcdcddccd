services:
  rnsd:
    build: .
    container_name: rns-reticulum-node
    restart: unless-stopped
    ports:
      - "4242:4242/tcp"
    volumes:
      - reticulum-data:/config

volumes:
  reticulum-data:
